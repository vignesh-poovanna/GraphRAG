# Core dependencies
neo4j
qdrant-client
sentence-transformers

# Multilingual embedding (Phase 3)
transformers
torch

# Chunking / format conversion
python-docx
markitdown
pdfminer.six

# LLM backends (Phase 7)
openai
ollama

# API backend (Phase 9)
fastapi
uvicorn[standard]
python-multipart

# Speech pipeline (Phase 8)
faster-whisper
kokoro-onnx
sounddevice
soundfile
langdetect

# Utilities
pyyaml
python-dotenv
numpy
